# GUI dependencies (install into the pulse-llava env).
# Narrative reports use a local Ollama model (see docs/ARCHITECTURE.md);
# RhythmCNN reuses the project's torch stack.
dash>=3.0
plotly>=5.20
reportlab>=4.0
pillow
numpy
matplotlib
scipy
neurokit2>=0.2.10
scikit-learn>=1.5
